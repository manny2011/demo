package com.example.administrator.biaozhunban.greendao;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.example.administrator.biaozhunban.greendao.entity.Nation;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "NATION".
*/
public class NationDao extends AbstractDao<Nation, Long> {

    public static final String TABLENAME = "NATION";

    /**
     * Properties of entity Nation.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Nation_id = new Property(0, Long.class, "nation_id", true, "_id");
        public final static Property Nation_name = new Property(1, String.class, "nation_name", false, "NATION_NAME");
        public final static Property CreateDate = new Property(2, String.class, "createDate", false, "CREATE_DATE");
    }


    public NationDao(DaoConfig config) {
        super(config);
    }
    
    public NationDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"NATION\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: nation_id
                "\"NATION_NAME\" TEXT NOT NULL ," + // 1: nation_name
                "\"CREATE_DATE\" TEXT);"); // 2: createDate
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"NATION\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, Nation entity) {
        stmt.clearBindings();
 
        Long nation_id = entity.getNation_id();
        if (nation_id != null) {
            stmt.bindLong(1, nation_id);
        }
        stmt.bindString(2, entity.getNation_name());
 
        String createDate = entity.getCreateDate();
        if (createDate != null) {
            stmt.bindString(3, createDate);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, Nation entity) {
        stmt.clearBindings();
 
        Long nation_id = entity.getNation_id();
        if (nation_id != null) {
            stmt.bindLong(1, nation_id);
        }
        stmt.bindString(2, entity.getNation_name());
 
        String createDate = entity.getCreateDate();
        if (createDate != null) {
            stmt.bindString(3, createDate);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public Nation readEntity(Cursor cursor, int offset) {
        Nation entity = new Nation( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // nation_id
            cursor.getString(offset + 1), // nation_name
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2) // createDate
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, Nation entity, int offset) {
        entity.setNation_id(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setNation_name(cursor.getString(offset + 1));
        entity.setCreateDate(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(Nation entity, long rowId) {
        entity.setNation_id(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(Nation entity) {
        if(entity != null) {
            return entity.getNation_id();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(Nation entity) {
        return entity.getNation_id() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
