<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:contentDescription="疾病详情查询"
    android:orientation="vertical">

    <include
        android:id="@id/widget_nav"
        layout="@layout/widget_activity_nav"
        android:layout_width="fill_parent"
        android:layout_height="@dimen/widget_size_nav_height" />

    <include
        android:id="@+id/searchbar"
        layout="@layout/widget_search_disease_bar"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/widget_nav" />

    <ListView
        android:id="@+id/listView"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_below="@id/searchbar" />
</RelativeLayout>